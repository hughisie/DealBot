# Core dependencies
requests>=2.31.0
pydantic>=2.5.0
python-dotenv>=1.0.0
jinja2>=3.1.0
pyyaml>=6.0
tenacity>=8.2.0
rich>=13.7.0
python-amazon-paapi>=5.0.0
openpyxl>=3.1.0

# Scheduling and timezone
pytz>=2023.3
APScheduler>=3.10.4

# Google Drive API
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Google Cloud Storage (for database persistence)
google-cloud-storage>=2.10.0

# Playwright for fallback Amazon scraping
playwright>=1.40.0
playwright-stealth>=1.0.0

# AI validation and product reviews
openai>=1.0.0  # For DeepSeek API (OpenAI-compatible)

# Optional GUI dependencies (not needed for daemon)
# toga>=0.4.0
# briefcase>=0.3.0
